// const swapPoints = (arr: number[], a: number, b: number) {
//     const temp = arr[a];
//     arr[a] = b;
//     arr[b] = temp;
// }
//
// export const run = (arr: number[]) => {
//     quickSort(arr, 0, arr.length-1);
// };
//
// export const quickSort = (arr: number[], start: number, end: number) => {
//     while (arr.slice(start, end).length) {
//         let pivot = Math.round((end - start)/2); // select middle for pivot
//
//     //    swap pivot to the end.
//
//     }
// //    while the length
// //    select a pivot
//
// //    Move the pivot to the end
//
//     // create start and end pointers.
//
//     // move the pointers inwards.
//
// //    if the left pointer is greater than right pointer or otherway. stop
//
//     //    if the left pointer is greater than right swap them.
//
// };
